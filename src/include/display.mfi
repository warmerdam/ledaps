/* This include file generates the correct FORTRAN syntax for including
   the processing message parameters. */
#ifdef vms
	include 'lasinclude:display.fin'
#else
	include '$LASINCLUDE/display.fin'
#endif
